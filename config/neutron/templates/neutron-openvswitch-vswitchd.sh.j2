#!/usr/bin/env bash
sleep 20
set -e
modprobe openvswitch
ovs-vsctl --no-wait show
chmod 777 /run/openvswitch/db.sock
bash /tmp/ovs-ensure-configured.sh {{ neutron_bridge_name }} {{ interface_name }}
/usr/sbin/ovs-vswitchd unix:/run/openvswitch/db.sock --mlockall
