apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: {{ deployment.namespace }}
  name: glance-api
spec:
  rules:
  - host: {{ network.ingress.host }}
    http:
      paths:
      -  backend:
          serviceName: glance-api
          servicePort: {{ network.port.api }}
