apiVersion: v1
kind: Service
metadata:
  namespace: {{ deployment.namespace }}
  name: glance-api
spec:
  ports:
  - name: keystone-api-public
    port: {{ network.port.api }}
  selector:
    app: glance-api
