apiVersion: v1
kind: Service
metadata:
  namespace: {{ variables.deployment.namespace }}
  name: glance-api
spec:
  ports:
  - name: keystone-api-public
    port: {{ variables.network.port.api }}
  selector:
    app: glance-api
