apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ variables.deployment.namespace }}
  name: glance-glanceswitchstoreconf
data:
  glance-swift-store.conf: |-
    [ref1]
    auth_address = {{ variables.rados_gateway.swift_auth }}
    key = {{ variables.rados_gateway.ceph_admin_keyring }}
    user = {{ variables.rados_gateway.swift_user }}
