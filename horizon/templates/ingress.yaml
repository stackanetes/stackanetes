apiVersion: extensions/v1beta1
kind: Ingress
spec:
  rules:
  - host: {{ network.ingress.named_host }}
    http:
      paths:
      -  backend:
          serviceName: horizon
          servicePort: {{ network.port }}
