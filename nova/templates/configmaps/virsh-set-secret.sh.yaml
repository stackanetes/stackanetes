apiVersion: v1
kind: ConfigMap
data:
  virsh-set-secret.sh: |-
    #!/bin/bash
    set -ex

    sleep 10

    KEY=$(ceph auth get-key client.{{ ceph.cinder_user }})
    virsh secret-define --file /tmp/secret.xml
    virsh secret-set-value --secret {{ ceph.secret_uuid }} --base64 $KEY && rm /tmp/secret.xml
