apiVersion: v1
kind: ConfigMap
data:
  config.yaml: |+
    IdentityEndpoint: {{ keystone.auth_uri }}/v3/
    Username: {{ keystone.admin_user }}
    Password: {{ keystone.admin_password }}
    TenantName: {{ drain.tenant_name }}
    DomainID: {{ drain.domain_id }}
