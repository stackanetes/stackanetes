apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: {{ deployment.namespace }}
  name: keystone-api
spec:
  rules:
  - host: {{ network.ingress.named_host }}
    http:
      paths:
      -  backend:
          serviceName: keystone-api
          servicePort: {{ network.port.public }}
